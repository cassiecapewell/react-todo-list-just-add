
const Todo = ({ todo }) => {
    return (
        <div>
            <h2>{todo.item}</h2>
        </div>
    )
}
// this makes todo components that will populate in the list in App.js

export default Todo
